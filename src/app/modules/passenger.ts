export class Passenger {
  constructor(public distanceKm: number, public age: number, public baggageWeight: number) {
    this.distanceKm = distanceKm,
    this.age = age,
    this.baggageWeight = baggageWeight
  }
}
